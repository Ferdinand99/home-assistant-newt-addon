#include <tunables/global>

profile 96282436_newt flags=(attach_disconnected,mediate_disconnected) {
  #include <abstractions/base>

  network,
  capability,
  file,
  /dev/net/tun rw,
  /dev/net/tun rwk,

  # Allow access to essential system files
  /run/dbus/system_bus_socket rw,
  /etc/nsswitch.conf r,
  /etc/resolv.conf r,
  
  # Allow the addon to run its own binary
  /** rkx,
}
